# easefilter-sys

These are raw bindings for EaseFilter generated using [bindgen](https://rust-lang.github.io/rust-bindgen/introduction.html).
The generated code is unsafe, and maps directly to the equivalent C functions.

The crate also includes utilities for interfacing with Windows.
